# Домашнее задание 13.2: Библиотеки csv и pandas

## Описание проекта
Проект представляет собой бэкенд для банковского виджета, отображающего последние транзакции. Реализовано тестирование кода с использованием `pytest` и `pytest-cov` для обеспечения качества и надежности.

## Структура тестирования

### Модули тестирования
- `tests/test_masks.py` - тесты для функций маскирования карт и счетов
- `tests/test_widget.py` - тесты для виджетов форматирования данных
- `tests/test_processing.py` - тесты для обработки транзакций
- `tests/conftest.py` - общие фикстуры для тестовых данных

### Используемые технологии
- **pytest** - фреймворк для тестирования
- **pytest-cov** - измерение покрытия кода тестами
- **flake8** - линтинг кода
- **mypy** - проверка типов
- **isort** - сортировка импортов

## Критерии выполнения

### Работа с GitHub
- Домашняя работа сдана через pull request из ветки домашней работы в ветку `develop`
- В коммиты не добавлены игнорируемые файлы
- README-файл дополнен информацией о тестировании

### Тестирование
- Написаны тесты ко всем функциям проекта
- Модули тестируются в отдельных тестовых файлах
- Функциональный код покрыт тестами более чем на 80%
- При запуске тестов командой `pytest` все тесты завершаются успешно
- В репозитории есть папка с отчетом покрытия тестами в формате HTML

### Фикстуры и параметризация
- В тестах используются фикстуры для генерации данных для теста
- В тестах используется параметризация для проверки различных кейсов работы функций

### Оформление кода
- При запуске линтеров Flake8 и mypy выдается не более 4 ошибок
- При вызове isort форматируется не более 1 импорта

## Команды для запуска

### Установка зависимостей
```
poetry install
```

## Запуск тестов

### Все тесты
```
poetry run pytest
```
### Тесты с подробным выводом
```
poetry run pytest -v
```
### Тесты с измерением покрытия
```
poetry run pytest --cov=src --cov-report=term-missing
```
### Генерация HTML отчета покрытия
```
poetry run pytest --cov=src --cov-report=html:coverage_html
```

## Фикстуры в conftest.py
- **sample_card_numbers()** - тестовые номера карт
- **sample_account_numbers()** - тестовые номера счетов
- **sample_dates()** - тестовые даты в различных форматах
- **sample_transactions()** - тестовые транзакции с разными статусами
- **empty_transactions()** - пустой список транзакций

## Модуль masks
- **get_mask_card_number:** Тестирование маскирования номеров карт, включая граничные случаи
- **get_mask_account:** Тестирование маскирования номеров счетов с различной длиной

## Модуль widget
- **mask_account_card:** Проверка распознавания типа данных и применения соответствующей маски
- **get_date:** Тестирование преобразования форматов дат

## Модуль processing
- **filter_by_state:** Фильтрация транзакций по статусу (`EXECUTED`, `PENDING`, `CANCELED`)
- **sort_by_date:** Сортировка транзакций по дате (возрастание/убывание)

## Модуль generators

Модуль `generators` предоставляет инструменты для работы с большими объемами данных транзакций 
с использованием генераторов Python. Генераторы позволяют эффективно обрабатывать данные, 
не загружая их全部 в память.

### Функции

#### `filter_by_currency(transactions, currency_code)`

**Фильтрует транзакции по коду валюты.**

Функция-генератор, которая проходит по списку транзакций и возвращает только те транзакции, 
валюта которых соответствует заданной.

**Параметры:**
- `transactions`: список словарей с транзакциями
- `currency_code`: код валюты для фильтрации (например, "USD", "RUB")

**Пример использования:**
```python```
from src.generators import filter_by_currency

## Получаем итератор для транзакций в USD
usd_transactions = filter_by_currency(transactions, "USD")

## Итерируем по транзакциям
for transaction in usd_transactions:
    print(f"ID: {transaction['id']}, Сумма: {transaction['operationAmount']['amount']}")

## Или используем next() для получения по одной транзакции
usd_iterator = filter_by_currency(transactions, "USD")
first_transaction = next(usd_iterator)

## Модуль decorators

### Описание
Новый модуль `src/decorators.py` предоставляет декоратор `log` для автоматического логирования работы функций. Декоратор позволяет регистрировать детали выполнения функций, включая успешные операции и ошибки.

### Декоратор log

#### `log(filename=None)`
**Автоматически логирует выполнение функций.**

Декоратор регистрирует начало и конец выполнения функции, а также результаты или возникшие ошибки.

**Параметры:**
- `filename` (Optional[str]): Имя файла для записи логов. Если None, логи выводятся в консоль.

**Возвращает:**
- Декоратор для функции

**Примеры использования:**

```python```
from src.decorators import log

## Логирование в консоль
@log()
def add_numbers(a: int, b: int) -> int:
    return a + b

## Логирование в файл
@log(filename="operations.log")
def process_text(text: str) -> str:
    return text.upper()

## Использование функций
result1 = add_numbers(5, 3)  # Лог в консоль: "2024-01-15 10:30:00 add_numbers ok"
result2 = process_text("hello")  # Лог в файл operations.log

## Отчёт по покрытости
После запуска тестов с генерацией HTML отчёта:
```
poetry run pytest --cov=src --cov-report=html:coverage_html
```
Отчёт будет доступен в появившейся папке. Открыть файл для просмотра можно в браузере, для детального изучения информации.

## Модули для работы с JSON и конвертацией валют

### Описание
Новые модули предоставляют функционал для чтения JSON-файлов с транзакциями и конвертации валют через внешнее API.

### Модуль utils_file_operations

#### `read_json_file(file_path)`
**Читает JSON-файл и возвращает список транзакций.**

**Параметры:**
- `file_path` (str): Путь к JSON-файлу

**Возвращает:**
- List[Dict]: Список словарей с транзакциями. Возвращает пустой список если файл не найден, пустой или содержит не список.

**Пример использования:**
```
from src.utils.file_operations import read_json_file

transactions = read_json_file("data/operations.json")
print(f"Загружено {len(transactions)} транзакций")
```
### Конфигурация логирования
Создан модуль `src/logging_config.py`, который предоставляет:

- **Отдельные логеры** для каждого модуля (`masks`, `utils`, `currency_converter`)
- **Запись логов в файлы** в папку `logs/` с расширением `.log`
- **Формат логов**: метка времени, название модуля, уровень серьезности, сообщение
- **Перезапись логов** при каждом запуске приложения

### Уровни логирования
- **DEBUG** - детальная информация для отладки
- **INFO** - подтверждение успешного выполнения операций
- **WARNING** - предупреждения о нестандартных ситуациях
- **ERROR** - ошибки выполнения операций

### Логируемые события

#### Модуль `masks`
- Начало и успешное завершение маскирования карт и счетов
- Ошибки валидации входных данных
- Некорректная длина номеров карт/счетов

#### Модуль `utils_file_operations`
- Извлечение суммы и валюты из транзакций
- Ошибки отсутствия обязательных ключей
- Ошибки преобразования типов данных

#### Модуль `currency_converter`
- Запросы к API курсов валют
- Успешное получение курсов обмена
- Сетевые ошибки и ошибки API
- Конвертация валют транзакций

## Используемые технологии
- **logging** - стандартная библиотека Python для логирования
- **pytest** - фреймворк для тестирования
- **pytest-cov** - измерение покрытия кода тестами
- **flake8** - линтинг кода
- **mypy** - проверка типов
- **isort** - сортировка импортов
- **requests** - HTTP-запросы к API курсов валют

## Критерии выполнения

### Работа с Git и GitHub
- Домашняя работа сдана через pull request из ветки домашней работы в ветку `develop`
- В коммиты не добавлены игнорируемые файлы
- README-файл дополнен информацией о логировании

## Новая функциональность: поддержка CSV и Excel файлов

Теперь система поддерживает чтение финансовых транзакций из:
- JSON файлов (существующая функциональность)
- CSV файлов
- Excel (XLSX) файлов

### Новые функции:
- `read_csv_file(file_path)` - чтение транзакций из CSV
- `read_excel_file(file_path, sheet_name=0)` - чтение транзакций из Excel

## Новый модуль search

### Описание
Модуль `search.py` предоставляет функции для поиска и анализа банковских операций с использованием регулярных выражений.

### Функции

#### `search_transactions(transactions, search_string)`
**Ищет транзакции по строке в описании.**

Использует регулярные выражения для регистронезависимого поиска.

**Параметры:**
- `transactions`: список словарей с транзакциями
- `search_string`: строка для поиска

from src.search import search_transactions

found = search_transactions(transactions, "Перевод")
print(f"Найдено {len(found)} транзакций")

### Установка зависимостей:
```bash
pip install pandas openpyxl

**Пример использования:**
```python
```